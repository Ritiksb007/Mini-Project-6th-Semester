# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting

on:
  # Choose your deployment trigger (uncomment the desired option):
  
  # Option 1: Deploy on every push to the Sohail-FrontEnd branch (for development)
  # push:
  #   branches:
  #     - Sohail-FrontEnd

  # Option 2: Deploy only on pull request merges to Sohail-FrontEnd branch
  pull_request:
    branches: [ Sohail-FrontEnd ]
    types: [ closed ]

  # Option 3: Deploy on specific tags pushed to the branch (e.g., v1.0)
  # push:
  #   tags:
  #     - 'v*'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Build step (adjust based on your project setup)
      - name: Build (replace with your specific build command)
        run: |  # Multi-line command support
          # Example build command for a React project (replace with your actual command)
          npm install
          npm run build

      - uses: FirebaseExtended/action-hosting-deploy@v0  # Assuming you've installed the action
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_STDPERFORMANCE_BF994 }}
          channelId: live  # You can change this to 'staging' for a staging deployment
          projectId: stdperformance-bf994
